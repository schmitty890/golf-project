REACT_APP_API_URL=http://localhost:5001

# Google Analytics 4 Measurement ID (G-XXXXXXXXXX). Left blank in dev so analytics stays off.
# Set this only in production (Netlify env vars). To test locally, fill it in and restart.
# REACT_APP_GA_ID=
